<h3>Artist Form</h3>

<%= form_for(@artist) do |f| %>
    <%= f.label "Name:" %>
    <%= f.text_field :name %><br>

    <%= f.label "Bio:" %>
    <%= f.text_area :bio %><br>

    <%= f.submit %>
<% end %>